# Build Stage
stages:
  - stage: Build
    displayName: Build
    jobs:
    - job: BuildJob
      displayName: Build Job
      steps:
      - script:  dotnet build --configuration $(buildConfiguration)