parameters:
- name: buildConfiguration
  type: string
  default: 'Release'


# Build Stage
stages:
  - stage: BuildJob
    displayName: Build
    jobs:
    - job: BuildJob
      displayName: Build Job
      steps:
      - script:  dotnet build --configuration ${{ variables.buildConfiguration }}