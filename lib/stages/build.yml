variables:
  template: ./vars.yml

# Build Stage
stages:
  - stage: BuildJob
    displayName: Build
    jobs:
    - job: BuildJob
      displayName: Build Job
      steps:
      - script:  echo dotnet build --configuration ${{ variables.buildConfiguration }}